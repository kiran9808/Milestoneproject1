# Groq (Phase 4) — copy to `.env` and fill in. Do not commit `.env`.
GROQ_API_KEY= 
# Optional: model id (default in code is llama-3.1-8b-instant)
# GROQ_MODEL=llama-3.3-70b-versatile
# Optional: override OpenAI-compatible base URL
# GROQ_BASE_URL=https://api.groq.com/openai/v1
