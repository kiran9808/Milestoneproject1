# Streamlit Community Cloud — runtime dependencies (API + recommendation engine).
# Do not add `datasets` here; use `pip install -e ".[ingest]"` locally for Phase 1 ingest only.
streamlit>=1.32.0,<2
fastapi>=0.109.0,<1
uvicorn>=0.27.0,<1
pydantic>=2.0.0,<3
python-dotenv>=1.0.0,<2
openai>=1.30.0,<3
