# Phase 5 — Output display

Stable JSON contract and **`POST /recommendations/ranked`** (see `routes.py`, `schema.py`).

**Implemented:** backend public fields `rating` / `cost`, `disclaimer`, `summary?`, `items[]`, `selection` meta, `used_llm_fallback`.

**Demo UI:** with the API running, open [http://127.0.0.1:8000/ui/](http://127.0.0.1:8000/ui/) (or `/` → redirects to `/ui/`). Static files live under repo `web/`.
